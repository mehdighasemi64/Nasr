export const initEmployeeList = item => ({ type: "INIT_EMPLOYEELIST", item: item});

export const initSelectionList = item => ({ type: "INIT_SELECTIONLIST", item: item});

export const addToSelectionList = item => ({ type: "ADD_SELECTIONLIST", item: item});

export const removeFromSelectionList = item => ({ type: "SUB_SELECTIONLIST", item: item});